const models = {
    usersModel : require('../models/nosql/users'),
    tracksModel : require('../models/nosql/tracks'),
    storageModel : require('../models/nosql/storage')
}

module.exports = models